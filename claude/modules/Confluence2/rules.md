# Confluence2 — Rules

1. Treat as a separate product with its own OpenAPI codegen workflow.
2. Auth is required on knowledge/ask routes — do not “simplify” by removing it when borrowing ideas.
3. Prefer `pnpm codegen` contract updates over hand-editing generated clients.
4. Out of scope for Atlas page work unless explicitly requested.
