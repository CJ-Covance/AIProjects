# JiraTestDataImporter — Summary

.NET Framework 4.8 console app that pulls Jira Cloud issues via JQL and upserts into SQL Server. Host kept thin for a possible future Web API.

Out of scope for Atlas UI/API page work.
